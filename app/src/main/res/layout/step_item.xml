<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/stepItem">

    <com.google.android.material.textview.MaterialTextView
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:id="@+id/stepDescription"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintRight_toLeftOf="@id/removeButton"
        app:layout_constraintLeft_toLeftOf="parent"
        android:textColor="@android:color/black"
        android:maxLines="5"
        tools:text="@string/stepsTitle"
        />
    <androidx.appcompat.widget.AppCompatImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/stepImage"
        app:layout_constraintTop_toBottomOf="@id/stepDescription"
        app:layout_constraintRight_toLeftOf="@id/removeButton"
        app:layout_constraintLeft_toLeftOf="parent"
        android:maxHeight="@dimen/image_max_size"
        android:scaleType="fitCenter"
        />
    <com.google.android.material.button.MaterialButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/removeButton"
        app:layout_constraintTop_toTopOf="@+id/stepDescription"
        app:layout_constraintBottom_toBottomOf="@id/stepDescription"
        app:layout_constraintLeft_toRightOf="@id/stepDescription"
        app:layout_constraintRight_toRightOf="parent"
        app:icon="@drawable/ic_baseline_remove_circle_24dp"
        android:checkable="false"
        style="@style/Widget.AppTheme.IconButton"
        />

</androidx.constraintlayout.widget.ConstraintLayout>
